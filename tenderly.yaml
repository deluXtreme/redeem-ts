account_id: ""
actions:
  bh2smith/project:
    runtime: v2
    sources: actions
    specs:
      block-redeemer:
        description: Example Web3 Action that logs block number once it is mined.
        function: index:blockWatchingRedeemer
        trigger:
          type: block
          block:
            network:
              - 100
            blocks: 500 # ~ 40 minutes with 5 second blocks.
project_slug: ""
